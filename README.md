# shorty
Rusty URL Shortener

## Setup

`nightly` is required for rocket library's features
```sh
rustup update
rustup override set nightly
```

## Build & Run

Just build:
```sh
cargo build
```

Build and run:
```sh
cargo run
```
